# info-quiz-backend

Backend service for the Info Quiz application.

## Setup

1. Install dependencies with `npm install`.
2. Copy `.env.example` values into a local `.env`.
3. Start the server with `npm start`.

## Environment Variables

- `PORT`
- `MONGODB_URI`
- `MONGODB_DB_NAME`
- `JWT_SECRET`
- `CLIENT_URLS`